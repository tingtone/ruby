<!DOCTYPE html>
<html>
  <head>
    <title><%=  yield(:title) %></title>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
    <meta name="keywords" content="<%= APPLICATION_CONFIG['meta_description'] %>" />
    <meta name="description" content="<%= APPLICATION_CONFIG['meta_description'] %>" />
    <link rel="shortcut icon" href="/favicon.png" />

    <%= stylesheet_link_tag( APPLICATION_CONFIG['stylesheet_screen'], :media => :screen )%>
    <%= stylesheet_link_tag( APPLICATION_CONFIG['stylesheet_print'], :media => :print )%>
    <%= javascript_include_tag :defaults%>
    <!-- prototype-lib will be loaded only when needed -->
    <%= yield :head %>
    <%= insert_google_analytics_script if  (Rails.env == 'production') %>
    <%= csrf_meta_tag %>
  </head>
  <body>
    <div id='overlay' class="ui-widget-content"></div>
    <div id="banner">
      <%= render :partial => '/layouts/banner' %>
    </div>

    <div id="session">
        <ul class="hmenu">
          <%= render '/layouts/forums/forum_menu' %>
        </ul>
        <ul class="hmenu" style='float: right;'>
          <%= render 'forum/menu/registration_items' %>
          <%= render 'forum/menu/login_items' %>
          <%= render '/layouts/forums/search' %>
        </ul>
    </div>


    
    <div id="container">
      <!--
      <div class='google-search'>
         %= insert_google_site_search %
      </div>
      -->

      <div>
        <% flash.each do |name, msg| %>
          <%= content_tag :div, raw(msg), :id => "flash_#{name}" %>
        <% end %>
      </div>

      
      <div style="margin: 0px;">
         <div id="cse" style="display: hidden; background: #fee;"></div>

        <%= yield %>
      </div>

      <div id='clear'></div>

    </div>

    <div id='copyright'>
        © <%= Time.now.year %> by <%= link_to APPLICATION_CONFIG['copyright'], APPLICATION_CONFIG['copyright_url'] %>
    </div>

  </body>
</html>
 