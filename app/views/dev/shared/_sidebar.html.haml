- if developer_signed_in?
  %ul.client_applications
    - current_developer.game_applications.each do |client_application|
      %li.client_application= client_application.name

    %li= link_to 'add new game', new_dev_game_application_path
