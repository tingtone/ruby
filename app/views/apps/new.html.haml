= title "Add App"

= simple_form_for @app, :as => :app, :url => developer_apps_path(@developer), :html => {:multipart => true,"data-ajax" => false} do |f|
  = render :partial => "regist_app", :locals => {:f => f}
  %div{"data-role" =>"fieldcontain"}
    = f.submit "Create"