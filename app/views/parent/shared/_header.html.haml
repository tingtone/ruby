- if parent_signed_in?
  = link_to t('my_info'), edit_parent_registration_path
  = link_to t('sign_out'), destroy_parent_session_path
- else
  = link_to t('sign_in'), new_parent_session_path
  = link_to t('sign_up'), new_parent_registration_path
