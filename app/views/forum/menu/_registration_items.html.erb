<% if forum_user_signed_in? %>
  <li>
  <em><%= link_to(current_forum_user.name, '#') %></em>
  </li>
<% else %>
  <li>
    <em><%= current_or_guest_user.name %></em>
  </li>
  <li>
  <%= link_to("sign_up", new_forum_user_registration_path)  %>
  </li>
<% end %>
