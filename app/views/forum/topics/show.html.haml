- content_for :title do
  = "#{@topic.title} - "

.forum.forum-detail
  .forum-top.cf
    = paginate @posts ,:theme=>"forum"

  .forum-mid
    %div
      %table{:width => '100%', :cellpadding => '0', :cellspacing => '0'}
        = render :partial => "topic_content"
        = render :partial => "post_item", :collection => @posts


  .forum-top.cf
    =# render 'reply'

